version: '3.8'

services:
  exectrace:
    build: .
    container_name: exectrace-server
    ports:
      - "8080:8080"
    volumes:
      - ./backend/data:/app/backend/data
    restart: unless-stopped
