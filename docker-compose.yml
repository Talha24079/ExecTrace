version: '3.8'

services:
  exectrace:
    build: .
    container_name: exectrace-server
    ports:
      - "9090:8080"
    volumes:
      - ./backend/data:/app/backend/data # Persist database files on host
    restart: unless-stopped
